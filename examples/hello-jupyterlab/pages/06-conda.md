---
title: Using conda
when: track == "conda"
optional: true
---

# Using conda

This page is only shown on the conda track, and is optional. There is no
conda in JupyterLite, so the action has nothing to do there.

```{execute}
:session: shell
conda --version
:lite:
```
