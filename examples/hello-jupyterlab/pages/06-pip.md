---
title: Using pip
when: track == "pip"
---

# Using pip

This page is only shown on the pip track.

```{execute}
:session: shell
python3 -m pip --version
:windows:
py -m pip --version
:lite:
echo "JupyterLite installs packages with micropip, not pip"
```
