/**
 * Name of the checkpoint the extension takes of a workshop's files the
 * first time it is opened, before any action runs. "Restart" restores it
 * to put the files back as they were, so a workshop cannot use the name
 * for a checkpoint of its own.
 */
export const PRISTINE_CHECKPOINT = 'pristine';
